
Navigate through the file dialog via WASD

Just show the favorite folders Name (not the path) in the list

If the Viewer gets launched directly (not by opening a splat), the file browser should be open by default

